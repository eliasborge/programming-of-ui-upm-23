import { FieldFocusDirective } from './field-focus.directive';

describe('FieldFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new FieldFocusDirective();
    expect(directive).toBeTruthy();
  });
});
