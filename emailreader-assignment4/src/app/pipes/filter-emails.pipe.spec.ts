import { FilterEmailsPipe } from './filter-emails.pipe';

describe('FilterEmailsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmailsPipe();
    expect(pipe).toBeTruthy();
  });
});
